<!-- @format -->

# DESIGN SYSTEM RULES

Always rely on ShadCN for components first, if that component isn't installed locally, install it via shadcn CLI. If the component isn't available in shadcn, then you can create it yourself, but make sure to follow the design system guidelines and use the same styling and structure as the existing components. Always prioritize consistency and reusability when creating new components.
