{
  "name": "rift26-ops",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "scripts": {
    "web": "bun --cwd apps/frontend dev",
    "worker": "bun --cwd apps/worker src/index.ts",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
